<%- include('../partials/header') %>

<h1 class="page-title">Community Events</h1>
<p>These are our key events you won't want to miss.</p>

<div class="event-list">
  <% events.forEach(event => { %>
    <div class="event-card <%= event.date === today ? 'today-event' : '' %>">
      <img src="<%= event.image %>" alt="<%= event.title %> Image" />
      <h2><%= event.title %></h2>
      <p><strong>Date:</strong> <%= event.date %></p>
      <p><strong>Location:</strong> <%= event.location %></p>
      <div class="event-description">
        <p><%= event.description %></p>
      </div>
    </div>
  <% }) %>
</div>

<h1 class="page-title">Featured Upcoming Events</h1>
<p>These events are just around the corner — don’t miss out!</p>

<div class="event-list">
  <% upcomingEvents.forEach(event => { %>
    <div class="event-card <%= event.date === today ? 'today-event' : '' %>">
      <img src="<%= event.image %>" alt="<%= event.title %> Image" />
      <h2><%= event.title %></h2>
      <p><strong>Date:</strong> <%= event.date %></p>
      <p><strong>Location:</strong> <%= event.location %></p>
      <div class="event-description">
        <p><%= event.description %></p>
      </div>
    </div>
  <% }) %>
</div>

<h1 class="page-title">Previous Events</h1>
<p>See what we've done together this year.</p>

<div class="event-list">
  <% previousEvents.forEach(event => { %>
    <div class="event-card">
      <img src="<%= event.image %>" alt="<%= event.title %> Image" />
      <h2><%= event.title %></h2>
      <p><strong>Date:</strong> <%= event.date %></p>
      <p><strong>Location:</strong> <%= event.location %></p>
      <div class="event-description">
        <p><%= event.description %></p>
      </div>
    </div>
  <% }) %>
</div>

<%- include('../partials/footer') %>


